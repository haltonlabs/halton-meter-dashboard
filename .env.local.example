# URL of the local Halton Meter daemon's HTTP API.
# Default matches `daemon.api_port = 8765` in ~/.halton-meter/config.toml.
# Override only if you've remapped the port or you're running the dashboard
# against a daemon on another host.
NEXT_PUBLIC_API_URL=http://localhost:8765
